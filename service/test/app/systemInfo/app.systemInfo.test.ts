import { expect } from 'chai'

describe('system information app interactions', function() {

  describe('reading system information', function() {

    it('includes the parsed version components', function() {
      expect.fail('todo')
    })
  })
})