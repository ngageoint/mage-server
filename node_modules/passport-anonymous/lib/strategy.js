/**
 * Module dependencies.
 */
var p***REMOVED***port = require('p***REMOVED***port-strategy')
  , util = require('util');


/**
 * Creates an instance of `Strategy`.
 *
 * The anonymous authentication strategy p***REMOVED***es authentication without verifying
 * credentials.
 *
 * Applications typically use this as a fallback on endpoints that can respond
 * to both authenticated and unauthenticated requests.  If credentials are not
 * supplied, this stategy p***REMOVED***es authentication while leaving `req.user` set to
 * `undefined`, allowing the route to handle unauthenticated requests as
 * desired.
 *
 * Examples:
 *
 *     p***REMOVED***port.use(new AnonymousStrategy());
 *
 * @constructor
 * @api public
 */
function Strategy() {
  p***REMOVED***port.Strategy.call(this);
  this.name = 'anonymous';
}

/**
 * Inherit from `p***REMOVED***port.Strategy`.
 */
util.inherits(Strategy, p***REMOVED***port.Strategy);

/**
 * P***REMOVED*** authentication without verifying credentials.
 *
 * @param {Object} req
 * @api protected
 */
Strategy.prototype.authenticate = function() {
  this.p***REMOVED***();
};


/**
 * Expose `Strategy`.
 */
module.exports = Strategy;
