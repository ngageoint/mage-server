var SecurityCredentialsNative = require('../../../build/Release/kerberos').SecurityCredentials;

// Add simple kebros helper
SecurityCredentialsNative.aquire_kerberos = function(username, p***REMOVED***word, domain, callback) {
  if(typeof p***REMOVED***word == 'function') {
    callback = p***REMOVED***word;
    p***REMOVED***word = null;
  } else if(typeof domain == 'function') {
    callback = domain;
    domain = null;
  }

  // We are going to use the async version
  if(typeof callback == 'function') {
    return SecurityCredentialsNative.aquire('Kerberos', username, p***REMOVED***word, domain, callback);
  } else {
    return SecurityCredentialsNative.aquireSync('Kerberos', username, p***REMOVED***word, domain);
  }
}

// Export the modified cl***REMOVED***
exports.SecurityCredentials = SecurityCredentialsNative;