require('../common');
var Gently = require('gently')
  , gently;

function test(test) {
  process.removeAllListeners('exit');
  gently = new Gently();
  test();
}

test(function constructor() {
  ***REMOVED***ert.deepEqual(gently.expectations, []);
  ***REMOVED***ert.deepEqual(gently.hijacked, {});
  ***REMOVED***ert.equal(gently.constructor.name, 'Gently');
});

test(function expectBadArgs() {
  var BAD_ARG = 'oh no';
  try {
    gently.expect(BAD_ARG);
    ***REMOVED***ert.ok(false, 'throw needs to happen');
  } catch (e) {
    ***REMOVED***ert.equal(e.message, 'Bad 1st argument for gently.expect(), object, function, or number expected, got: '+(typeof BAD_ARG));
  }
});

test(function expectObjMethod() {
  var OBJ = {}, NAME = 'foobar';
  OBJ.foo = function(x) {
    return x;
  };

  gently._name = function() {
    return NAME;
  };

  var original = OBJ.foo
    , stubFn = function() {};

  (function testAddOne() {
    ***REMOVED***ert.strictEqual(gently.expect(OBJ, 'foo', stubFn), original);

    ***REMOVED***ert.equal(gently.expectations.length, 1);
    var expectation = gently.expectations[0];
    ***REMOVED***ert.strictEqual(expectation.obj, OBJ);
    ***REMOVED***ert.strictEqual(expectation.method, 'foo');
    ***REMOVED***ert.strictEqual(expectation.stubFn, stubFn);
    ***REMOVED***ert.strictEqual(expectation.name, NAME);
    ***REMOVED***ert.strictEqual(OBJ.foo._original, original);
  })();

  (function testAddTwo() {
    gently.expect(OBJ, 'foo', 2, stubFn);
    ***REMOVED***ert.equal(gently.expectations.length, 2);
    ***REMOVED***ert.strictEqual(OBJ.foo._original, original);
  })();

  (function testAddOneWithoutMock() {
    gently.expect(OBJ, 'foo');
    ***REMOVED***ert.equal(gently.expectations.length, 3);
  })();

  var stubFnCalled = 0, SELF = {};
  gently._stubFn = function(self, obj, method, name, args) {
    stubFnCalled++;
    ***REMOVED***ert.strictEqual(self, SELF);
    ***REMOVED***ert.strictEqual(obj, OBJ);
    ***REMOVED***ert.strictEqual(method, 'foo');
    ***REMOVED***ert.strictEqual(name, NAME);
    ***REMOVED***ert.deepEqual(args, [1, 2]);
    return 23;
  };
  ***REMOVED***ert.equal(OBJ.foo.apply(SELF, [1, 2]), 23);
  ***REMOVED***ert.equal(stubFnCalled, 1);
});

test(function expectClosure() {
  var NAME = 'MY CLOSURE';
  function closureFn() {};

  gently._name = function() {
    return NAME;
  };

  var fn = gently.expect(closureFn);
  ***REMOVED***ert.equal(gently.expectations.length, 1);
  var expectation = gently.expectations[0];
  ***REMOVED***ert.strictEqual(expectation.obj, null);
  ***REMOVED***ert.strictEqual(expectation.method, null);
  ***REMOVED***ert.strictEqual(expectation.stubFn, closureFn);
  ***REMOVED***ert.strictEqual(expectation.name, NAME);

  var stubFnCalled = 0, SELF = {};
  gently._stubFn = function(self, obj, method, name, args) {
    stubFnCalled++;
    ***REMOVED***ert.strictEqual(self, SELF);
    ***REMOVED***ert.strictEqual(obj, null);
    ***REMOVED***ert.strictEqual(method, null);
    ***REMOVED***ert.strictEqual(name, NAME);
    ***REMOVED***ert.deepEqual(args, [1, 2]);
    return 23;
  };
  ***REMOVED***ert.equal(fn.apply(SELF, [1, 2]), 23);
  ***REMOVED***ert.equal(stubFnCalled, 1);
});

test(function expectClosureCount() {
  var stubFnCalled = 0;
  function closureFn() {stubFnCalled++};

  var fn = gently.expect(2, closureFn);
  ***REMOVED***ert.equal(gently.expectations.length, 1);
  fn();
  ***REMOVED***ert.equal(gently.expectations.length, 1);
  fn();
  ***REMOVED***ert.equal(stubFnCalled, 2);
});

test(function restore() {
  var OBJ = {}, NAME = '[my object].myFn()';
  OBJ.foo = function(x) {
    return x;
  };

  gently._name = function() {
    return NAME;
  };

  var original = OBJ.foo;
  gently.expect(OBJ, 'foo');
  gently.restore(OBJ, 'foo');
  ***REMOVED***ert.strictEqual(OBJ.foo, original);

  (function testError() {
    try {
      gently.restore(OBJ, 'foo');
      ***REMOVED***ert.ok(false, 'throw needs to happen');
    } catch (e) {
      ***REMOVED***ert.equal(e.message, NAME+' is not gently stubbed');
    }
  })();
});

test(function _stubFn() {
  var OBJ1 = {toString: function() {return '[OBJ 1]'}}
    , OBJ2 = {toString: function() {return '[OBJ 2]'}, foo: function () {return 'bar';}}
    , SELF = {};

  gently.expect(OBJ1, 'foo', function(x) {
    ***REMOVED***ert.strictEqual(this, SELF);
    return x * 2;
  });

  ***REMOVED***ert.equal(gently._stubFn(SELF, OBJ1, 'foo', 'dummy_name', [5]), 10);

  (function testAutorestore() {
    ***REMOVED***ert.equal(OBJ2.foo(), 'bar');

    gently.expect(OBJ2, 'foo', function() {
      return 'stubbed foo';
    });

    gently.expect(OBJ2, 'foo', function() {
      return "didn't restore yet";
    });

    ***REMOVED***ert.equal(gently._stubFn(SELF, OBJ2, 'foo', 'dummy_name', []), 'stubbed foo');
    ***REMOVED***ert.equal(gently._stubFn(SELF, OBJ2, 'foo', 'dummy_name', []), "didn't restore yet");
    ***REMOVED***ert.equal(OBJ2.foo(), 'bar');
    ***REMOVED***ert.deepEqual(gently.expectations, []);
  })();

  (function testNoMoreCallExpected() {
    try {
      gently._stubFn(SELF, OBJ1, 'foo', 'dummy_name', [5]);
      ***REMOVED***ert.ok(false, 'throw needs to happen');
    } catch (e) {
      ***REMOVED***ert.equal(e.message, 'Unexpected call to dummy_name, no call was expected');
    }
  })();

  (function testDifferentCallExpected() {
    gently.expect(OBJ2, 'bar');
    try {
      gently._stubFn(SELF, OBJ1, 'foo', 'dummy_name', [5]);
      ***REMOVED***ert.ok(false, 'throw needs to happen');
    } catch (e) {
      ***REMOVED***ert.equal(e.message, 'Unexpected call to dummy_name, expected call to '+gently._name(OBJ2, 'bar'));
    }

    ***REMOVED***ert.equal(gently.expectations.length, 1);
  })();

  (function testNoMockCallback() {
    OBJ2.bar();
    ***REMOVED***ert.equal(gently.expectations.length, 0);
  })();
});

test(function stub() {
  var LOCATION = './my_cl***REMOVED***';

  (function testRegular() {
    var Stub = gently.stub(LOCATION);
    ***REMOVED***ert.ok(Stub instanceof Function);
    ***REMOVED***ert.strictEqual(gently.hijacked[LOCATION], Stub);
    ***REMOVED***ert.ok(Stub['new'] instanceof Function);
    ***REMOVED***ert.equal(Stub.toString(), 'require('+JSON.stringify(LOCATION)+')');

    (function testConstructor() {
      var newCalled = 0
        , STUB
        , ARGS = ['foo', 'bar'];

      Stub['new'] = function(a, b) {
        ***REMOVED***ert.equal(a, ARGS[0]);
        ***REMOVED***ert.equal(b, ARGS[1]);
        newCalled++;
        STUB = this;
      };

      var stub = new Stub(ARGS[0], ARGS[1]);
      ***REMOVED***ert.strictEqual(stub, STUB);
      ***REMOVED***ert.equal(newCalled, 1);
      ***REMOVED***ert.equal(stub.toString(), 'require('+JSON.stringify(LOCATION)+')');
    })();

    (function testUseReturnValueAsInstance() {
      var R = {};

      Stub['new'] = function() {
        return R;
      };

      var stub = new Stub();
      ***REMOVED***ert.strictEqual(stub, R);

    })();
  })();

  var EXPORTS_NAME = 'MyCl***REMOVED***';
  test(function testExportsName() {
    var Stub = gently.stub(LOCATION, EXPORTS_NAME);
    ***REMOVED***ert.strictEqual(gently.hijacked[LOCATION][EXPORTS_NAME], Stub);
    ***REMOVED***ert.equal(Stub.toString(), 'require('+JSON.stringify(LOCATION)+').'+EXPORTS_NAME);

    (function testConstructor() {
      var stub = new Stub();
      ***REMOVED***ert.equal(Stub.toString(), 'require('+JSON.stringify(LOCATION)+').'+EXPORTS_NAME);
    })();
  });
});

test(function hijack() {
  var LOCATION = './foo'
    , REQUIRE_CALLS = 0
    , EXPORTS = {}
    , REQUIRE = function() {
        REQUIRE_CALLS++;
        return EXPORTS;
      };

  var hijackedRequire = gently.hijack(REQUIRE);
  hijackedRequire(LOCATION);
  ***REMOVED***ert.strictEqual(gently.hijacked[LOCATION], EXPORTS);

  ***REMOVED***ert.equal(REQUIRE_CALLS, 1);

  // make sure we are caching the hijacked module
  hijackedRequire(LOCATION);
  ***REMOVED***ert.equal(REQUIRE_CALLS, 1);
});

test(function verify() {
  var OBJ = {toString: function() {return '[OBJ]'}};
  gently.verify();

  gently.expect(OBJ, 'foo');
  try {
    gently.verify();
    ***REMOVED***ert.ok(false, 'throw needs to happen');
  } catch (e) {
    ***REMOVED***ert.equal(e.message, 'Expected call to [OBJ].foo() did not happen');
  }

  try {
    gently.verify('foo');
    ***REMOVED***ert.ok(false, 'throw needs to happen');
  } catch (e) {
    ***REMOVED***ert.equal(e.message, 'Expected call to [OBJ].foo() did not happen (foo)');
  }
});

test(function processExit() {
  var verifyCalled = 0;
  gently.verify = function(msg) {
    verifyCalled++;
    ***REMOVED***ert.equal(msg, 'process exit');
  };

  process.emit('exit');
  ***REMOVED***ert.equal(verifyCalled, 1);
});

test(function _name() {
  (function testNamedCl***REMOVED***() {
    function Foo() {};
    var foo = new Foo();
    ***REMOVED***ert.equal(gently._name(foo, 'bar'), '[Foo].bar()');
  })();

  (function testToStringPreference() {
    function Foo() {};
    Foo.prototype.toString = function() {
      return '[Superman 123]';
    };
    var foo = new Foo();
    ***REMOVED***ert.equal(gently._name(foo, 'bar'), '[Superman 123].bar()');
  })();

  (function testUnamedCl***REMOVED***() {
    var Foo = function() {};
    var foo = new Foo();
    ***REMOVED***ert.equal(gently._name(foo, 'bar'), foo.toString()+'.bar()');
  })();

  (function testNamedClosure() {
    function myClosure() {};
    ***REMOVED***ert.equal(gently._name(null, null, myClosure), myClosure.name+'()');
  })();

  (function testUnamedClosure() {
    var myClosure = function() {2+2 == 5};
    ***REMOVED***ert.equal(gently._name(null, null, myClosure), '>> '+myClosure.toString()+' <<');
  })();
});

test(function verifyExpectNone() {
  var OBJ = {toString: function() {return '[OBJ]'}};
  gently.verify();

  gently.expect(OBJ, 'foo', 0);
  try {
    gently.verify();
  } catch (e) {
    ***REMOVED***ert.fail('Exception should not have been thrown');
  }
});