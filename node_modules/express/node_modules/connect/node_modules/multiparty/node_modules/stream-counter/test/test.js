var ByteCounter = require('../');
var fs = require('fs');
var path = require('path');
var ***REMOVED***ert = require('***REMOVED***ert');

var counter = new ByteCounter();
var remainingTests = 2;
counter.once('progress', function() {
  ***REMOVED***ert.strictEqual(counter.bytes, 5);
  remainingTests -= 1;
});
var is = fs.createReadStream(path.join(__dirname, 'test.txt'));
is.pipe(counter);
is.on('end', function() {
  remainingTests -= 1;
  ***REMOVED***ert.strictEqual(counter.bytes, 5);
});
process.on('exit', function() {
  ***REMOVED***ert.strictEqual(remainingTests, 0);
});
