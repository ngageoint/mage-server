
var ***REMOVED***ert = require('***REMOVED***ert');

var cookie = require('..');

suite('parse');

test('basic', function() {
    ***REMOVED***ert.deepEqual({ foo: 'bar' }, cookie.parse('foo=bar'));
    ***REMOVED***ert.deepEqual({ foo: '123' }, cookie.parse('foo=123'));
});

test('ignore spaces', function() {
    ***REMOVED***ert.deepEqual({ FOO: 'bar', baz: 'raz' },
            cookie.parse('FOO    = bar;   baz  =   raz'));
});

test('escaping', function() {
    ***REMOVED***ert.deepEqual({ foo: 'bar=123456789&name=Magic+Mouse' },
            cookie.parse('foo="bar=123456789&name=Magic+Mouse"'));

    ***REMOVED***ert.deepEqual({ email: ' ",;/' },
            cookie.parse('email=%20%22%2c%3b%2f'));
});

test('ignore escaping error and return original value', function() {
    ***REMOVED***ert.deepEqual({ foo: '%1', bar: 'bar' }, cookie.parse('foo=%1;bar=bar'));
});
