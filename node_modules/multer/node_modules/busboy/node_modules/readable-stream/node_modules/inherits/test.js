var inherits = require('./inherits.js')
var ***REMOVED***ert = require('***REMOVED***ert')

function test(c) {
  ***REMOVED***ert(c.constructor === Child)
  ***REMOVED***ert(c.constructor.super_ === Parent)
  ***REMOVED***ert(Object.getPrototypeOf(c) === Child.prototype)
  ***REMOVED***ert(Object.getPrototypeOf(Object.getPrototypeOf(c)) === Parent.prototype)
  ***REMOVED***ert(c instanceof Child)
  ***REMOVED***ert(c instanceof Parent)
}

function Child() {
  Parent.call(this)
  test(this)
}

function Parent() {}

inherits(Child, Parent)

var c = new Child
test(c)

console.log('ok')
