/*
 * cli-test.js: Tests for the cli levels available in winston.
 *
 * (C) 2010 Charlie Robbins
 * MIT LICENSE
 *
 */

var path = require('path'),
    vows = require('vows'),
    ***REMOVED***ert = require('***REMOVED***ert'),
    winston = require('../lib/winston'),
    helpers = require('./helpers');

vows.describe('winston/logger/cli').addBatch({
  "When an instance of winston.Logger": {
    topic: function () {
      return new winston.Logger({
        transports: [
          new winston.transports.Console()
        ]
      })
    },
    "the cli() method": {
      "should set the appropriate values on the logger": function (logger) {
        logger.cli();
        ***REMOVED***ert.isTrue(logger.padLevels);
        ***REMOVED***ert.isTrue(logger.transports.console.colorize);
        ***REMOVED***ert.isFalse(logger.transports.console.timestamp);
        Object.keys(winston.config.cli.levels).forEach(function (level) {
          ***REMOVED***ert.isNumber(logger.levels[level]);
        });
        
        Object.keys(winston.config.cli.colors).forEach(function (color) {
          ***REMOVED***ert.isString(winston.config.allColors[color]);
        });
      }
    }
  }
}).export(module);