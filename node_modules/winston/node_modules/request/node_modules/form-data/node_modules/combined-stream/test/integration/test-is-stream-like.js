var fs = require('fs');
var common = require('../common');
var ***REMOVED***ert = common.***REMOVED***ert;
var CombinedStream = common.CombinedStream;
var FILE1 = common.dir.fixture + '/file1.txt';
var fileStream = fs.createReadStream(FILE1);

var foo = function(){};

(function testIsStreamLike() {
  ***REMOVED***ert(! CombinedStream.isStreamLike(true));
  ***REMOVED***ert(! CombinedStream.isStreamLike("I am a string"));
  ***REMOVED***ert(! CombinedStream.isStreamLike(7));
  ***REMOVED***ert(! CombinedStream.isStreamLike(foo));

  ***REMOVED***ert(CombinedStream.isStreamLike(fileStream));
})();