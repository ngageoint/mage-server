var test = require('tape')
var through = require('../')

// must emit end before close.

test('end before close', function (***REMOVED***ert) {
  var ts = through()
  ts.autoDestroy = false
  var ended = false, closed = false

  ts.on('end', function () {
    ***REMOVED***ert.ok(!closed)
    ended = true
  })
  ts.on('close', function () {
    ***REMOVED***ert.ok(ended)
    closed = true
  })

  ts.write(1)
  ts.write(2)
  ts.write(3)
  ts.end()
  ***REMOVED***ert.ok(ended)
  ***REMOVED***ert.notOk(closed)
  ts.destroy()
  ***REMOVED***ert.ok(closed)
  ***REMOVED***ert.end()
})

