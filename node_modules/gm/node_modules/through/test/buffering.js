var test = require('tape')
var through = require('../')

// must emit end before close.

test('buffering', function(***REMOVED***ert) {
  var ts = through(function (data) {
    this.queue(data)
  }, function () {
    this.queue(null)
  })

  var ended = false,  actual = []

  ts.on('data', actual.push.bind(actual))
  ts.on('end', function () {
    ended = true
  })

  ts.write(1)
  ts.write(2)
  ts.write(3)
  ***REMOVED***ert.deepEqual(actual, [1, 2, 3])
  ts.pause()
  ts.write(4)
  ts.write(5)
  ts.write(6)
  ***REMOVED***ert.deepEqual(actual, [1, 2, 3])
  ts.resume()
  ***REMOVED***ert.deepEqual(actual, [1, 2, 3, 4, 5, 6])
  ts.pause()
  ts.end()
  ***REMOVED***ert.ok(!ended)
  ts.resume()
  ***REMOVED***ert.ok(ended)
  ***REMOVED***ert.end()
})

test('buffering has data in queue, when ends', function (***REMOVED***ert) {

  /*
   * If stream ends while paused with data in the queue,
   * stream should still emit end after all data is written
   * on resume.
   */

  var ts = through(function (data) {
    this.queue(data)
  }, function () {
    this.queue(null)
  })

  var ended = false,  actual = []

  ts.on('data', actual.push.bind(actual))
  ts.on('end', function () {
    ended = true
  })

  ts.pause()
  ts.write(1)
  ts.write(2)
  ts.write(3)
  ts.end()
  ***REMOVED***ert.deepEqual(actual, [], 'no data written yet, still paused')
  ***REMOVED***ert.ok(!ended, 'end not emitted yet, still paused')
  ts.resume()
  ***REMOVED***ert.deepEqual(actual, [1, 2, 3], 'resumed, all data should be delivered')
  ***REMOVED***ert.ok(ended, 'end should be emitted once all data was delivered')
  ***REMOVED***ert.end();
})
