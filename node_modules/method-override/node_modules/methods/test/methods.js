var http = require('http');
var ***REMOVED***ert = require('***REMOVED***ert');
var methods = require('..');

describe('methods', function() {

  if (http.METHODS) {

    it('is a lowercased http.METHODS', function() {
      var lowercased = http.METHODS.map(function(method) {
        return method.toLowerCase();
      });
      ***REMOVED***ert.deepEqual(lowercased, methods);
    });

  } else {

    it('contains GET, POST, PUT, and DELETE', function() {
      ***REMOVED***ert.notEqual(methods.indexOf('get'), -1);
      ***REMOVED***ert.notEqual(methods.indexOf('post'), -1);
      ***REMOVED***ert.notEqual(methods.indexOf('put'), -1);
      ***REMOVED***ert.notEqual(methods.indexOf('delete'), -1);
    });

    it('is all lowercase', function() {
      for (var i = 0; i < methods.length; i ++) {
        ***REMOVED***ert(methods[i], methods[i].toLowerCase(), methods[i] + " isn't all lowercase");
      }
    });

  }

});
