
var ***REMOVED***ert = require('***REMOVED***ert')
var clone = require('../');

describe('regexp-clone', function(){
  function hasEqualSource (a, b) {
    ***REMOVED***ert.ok(a !== b);
    ***REMOVED***ert.equal(a.source, b.source);
  }

  function isInsensitive (a) {
    ***REMOVED***ert.ok(a.ignoreCase);
  }

  function isGlobal (a) {
    ***REMOVED***ert.ok(a.global);
  }

  function isMultiline (a) {
    ***REMOVED***ert.ok(a.multiline);
  }

  function insensitiveFlag (a) {
    var b = clone(a);
    hasEqualSource(a, b);
    isInsensitive(a);
    isInsensitive(b);
  }

  function globalFlag (a) {
    var b = clone(a);
    hasEqualSource(a, b);
    isGlobal(a);
    isGlobal(b);
  }

  function multilineFlag (a) {
    var b = clone(a);
    hasEqualSource(a, b);
    isMultiline(a);
    isMultiline(b);
  }

  describe('literals', function(){
    it('insensitive flag', function(done){
      var a = /hello/i;
      insensitiveFlag(a);
      done();
    })
    it('global flag', function(done){
      var a = /hello/g;
      globalFlag(a);
      done();
    })
    it('multiline flag', function(done){
      var a = /hello/m;
      multilineFlag(a);
      done();
    })
    it('no flags', function(done){
      var a = /hello/;
      var b = clone(a);
      hasEqualSource(a, b);
      ***REMOVED***ert.ok(!a.insensitive);
      ***REMOVED***ert.ok(!a.global);
      ***REMOVED***ert.ok(!a.global);
      done();
    })
    it('all flags', function(done){
      var a = /hello/gim;
      insensitiveFlag(a);
      globalFlag(a);
      multilineFlag(a);
      done();
    })
  })

  describe('instances', function(){
    it('insensitive flag', function(done){
      var a = new RegExp('hello', 'i');
      insensitiveFlag(a);
      done();
    })
    it('global flag', function(done){
      var a = new RegExp('hello', 'g');
      globalFlag(a);
      done();
    })
    it('multiline flag', function(done){
      var a = new RegExp('hello', 'm');
      multilineFlag(a);
      done();
    })
    it('no flags', function(done){
      var a = new RegExp('hmm');
      var b = clone(a);
      hasEqualSource(a, b);
      ***REMOVED***ert.ok(!a.insensitive);
      ***REMOVED***ert.ok(!a.global);
      ***REMOVED***ert.ok(!a.global);
      done();
    })
    it('all flags', function(done){
      var a = new RegExp('hello', 'gim');
      insensitiveFlag(a);
      globalFlag(a);
      multilineFlag(a);
      done();
    })
  })
})

