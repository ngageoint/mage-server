diff --git a/lib/model.js b/lib/model.js
index a76e56c..f848ad0 100644
--- a/lib/model.js
+++ b/lib/model.js
@@ -2178,6 +2178,7 @@ function populate (model, docs, options, cb) {
   // if a limit option is p***REMOVED***ed, we should have the limit apply to *each*
   // document, not apply in the aggregate
   if (options.options && options.options.limit) {
+    ***REMOVED***ignmentOpts.originalLimit = options.options.limit;
     options.options.limit = options.options.limit * len;
   }
 
@@ -2292,6 +2293,9 @@ function valueFilter (val, ***REMOVED***ignmentOpts) {
       if (!isDoc(subdoc)) continue;
       maybeRemoveId(subdoc, ***REMOVED***ignmentOpts);
       ret.push(subdoc);
+      if (i + 1 >= ***REMOVED***ignmentOpts.originalLimit) {
+        break;
+      }
     }
     return ret;
   }
